[pytest]
testpaths = tests
asyncio_mode = auto
addopts = 
    -v
    --tb=short
    --strict-markers
    --cov=custom_components.utility_tariff
    --cov-report=term-missing
    --cov-report=xml
    --cov-report=html
markers =
    asyncio: mark test as async